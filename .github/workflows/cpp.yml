name: C++ CI

on:
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]

jobs:
  build-ircserv:
    name: Build ircserv

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - run: make

  build-ircbot:
    name: Build ircbot

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - run: make
